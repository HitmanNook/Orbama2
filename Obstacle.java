import processing.core.PImage;

public class Obstacle extends Thing{
    //PImage image;
    int level, imageNum;
    public Obstacle(int level, int i){
        super(level, i);
        // imageNum = i;
        // image = img;

    }
}
