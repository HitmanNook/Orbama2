import processing.core.PImage;

public class Enemy extends Thing{
    //PImage image;
    int level, imageNum;
    public Enemy(int level, int i){
        super(level, i);
       // imageNum = i;
       // image = img;

    }
}
